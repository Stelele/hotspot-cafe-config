client localhost {
    ipaddr = 127.0.0.1
    secret = {{RADIUS_SECRET}}
    require_message_authenticator = yes
}

client wg-lan {
    ipaddr = 10.8.0.0/24
    secret = {{RADIUS_SECRET}}
    require_message_authenticator = yes
}
